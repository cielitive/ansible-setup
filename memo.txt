msg: "/etc/sudoers.d/{{ user | regex_replace('\\d.*', '') }}"

### template
{{ item.name }} systemctl * {{ item.name | regex_replace('^[a-z]+0+', '') }}.service

firewall-cmd --add-rich-rule='rule family="ipv4" source address="10.16.20.0/24" protocol value="icmp" accept' --permanent

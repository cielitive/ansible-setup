[targets]
node01 ansible_host=192.168.8.100

[targets:vars]
ansible_tag=targets

[all:vars]
# ansible_ssh_user="{{ lookup('env','USER') }}"
# ansible_ssh_pass="{{ lookup('env','SSH_PASSWD') }}"
# ansible_become_pass="{{ lookup('env','BECOME_PASSWD') }}"
